<Window x:Class="TemplateRevit2025.FamilyMananger.FamilyManagerWpf"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TemplateRevit2025.FamilyMananger"
        mc:Ignorable="d"
        Title="FamilyManagerWpf" Height="450" Width="800">
    <Window.Resources>
        <Style x:Key="listBoxStyle" TargetType="ListBox">
            <Setter Property="ItemsPanel">
                <Setter.Value>
                    <ItemsPanelTemplate>
                        <WrapPanel/>
                    </ItemsPanelTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <ListBox Name="listBoxFamilyCategory">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Foreground="Violet" Text="{Binding Name}"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <Grid Grid.Column="1" >
            <ListBox Style="{StaticResource listBoxStyle}" Name="listBoxFamily">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid Width="150" Height="150" >
                            <Grid.RowDefinitions>
                                <RowDefinition/>
                                <RowDefinition/>
                            </Grid.RowDefinitions>
                            <Image Source="{Binding Image}"/>
                            <TextBlock Text="{Binding NameFamily}" />
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>
    </Grid>
</Window>
